// First 10 multiples of a number
let n = 7;
for (let i = 1; i <= 10; i++) {
  console.log(`${n} x ${i} = ${n * i}`);
}

// ${n} → number (yaha 7)

// ${i} → loop counter (1 se 10 tak)

// ${n * i} → dono ka multiplication